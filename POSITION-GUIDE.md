# 📐 PDF 元件位置指南

## 🎯 座標系統

### 預覽器尺寸
- **寬度**: 595px
- **高度**: 842px
- **左邊距**: 40px
- **右邊距**: 40px
- **右邊界**: 555px (595 - 40)
- **中心點**: 297.5px (595 / 2)

### PDF 尺寸 (A4)
- **寬度**: 210mm
- **高度**: 297mm
- **轉換比例 X**: 0.352941 (210/595)
- **轉換比例 Y**: 0.352734 (297/842)

## 📊 元件位置對照表

| 元件 ID | 對齊方式 | left (px) | top (px) | 說明 |
|---------|---------|-----------|----------|------|
| `logo` | 左對齊 | 40 | 40 | 公司 Logo，左上角 |
| `company-info` | **右對齊** | **555** | 40 | 公司資訊，右上角 |
| `title` | 居中對齊 | 297.5 | 150 | 報價單/發票標題 |
| `separator-line` | 左對齊 | 40 | 190 | 分隔線 |
| `invoice-info` | 左對齊 | 40 | 210 | Invoice No, Date |
| `customer-info` | **右對齊** | **555** | 210 | Customer, Due Date |
| `customer-detail` | 左對齊 | 40 | 280 | 客戶詳細資訊框 |
| `items-table` | 左對齊 | 40 | 380 | 項目表格 |
| `total` | **右對齊** | **555** | 480 | 總計金額框 |
| `notes` | 左對齊 | 40 | 762 | 備註 |

## ⚠️ 重要概念

### 左對齊元件
`left` 值 = **左邊界的 X 座標**
```
┌─────────────────────
│ [元件內容]
│  ^
│  └─ left = 40px (左邊界)
```

### 右對齊元件
`left` 值 = **右邊界的 X 座標**
```
─────────────────────┐
        [元件內容]   │
                ^    │
                └─ left = 555px (右邊界)
```

### 居中對齊元件
`left` 值 = **中心點的 X 座標**
```
────────┌────────────┐────────
        │  [元件內容]  │
           ^
           └─ left = 297.5px (中心點)
```

## 🔧 如何修復位置

### 方法 1: 使用修復工具
1. 打開 `fix-positions-now.html`
2. 點擊 "立即修復位置"
3. 重新生成 PDF 測試

### 方法 2: 使用診斷工具
1. 打開 `diagnose-positions.html`
2. 點擊 "載入並診斷位置"
3. 查看哪些元件位置不正確
4. 使用修復工具或手動調整

### 方法 3: 手動調整
在 PDF 排版控制器中，確保：
- 左對齊元件的拖動框左邊界 = 40px
- 右對齊元件的拖動框右邊界 = 555px
- 居中對齊元件的中心點 = 297.5px

## 🐛 常見錯誤

### ❌ 錯誤 1: 右對齊元件使用左邊界值
```javascript
// 錯誤
'company-info': { left: 305 }  // ❌ 這是左邊界，不是右邊界

// 正確
'company-info': { left: 555 }  // ✅ 右邊界
```

### ❌ 錯誤 2: 混淆預覽器和 PDF 座標
預覽器使用 **像素 (px)**，PDF 使用 **毫米 (mm)**
- 預覽器: 555px
- PDF: 195.88mm (555 × 0.352941)

### ❌ 錯誤 3: 沒考慮元件寬度
右對齊元件的 `left` 是右邊界，實際內容會向左延伸：
```
totalX = 555px (右邊界)
totalWidth = 60mm
總計框範圍 = [totalX - totalWidth, totalX] = [108.12mm, 195.88mm]
```

## 📝 程式碼參考

### documents.js 中的對齊方式

```javascript
// 左對齊
pdf.text('Invoice No:', invoiceX, invoiceY);

// 右對齊
pdf.text(contact.name, customerX, customerY, { align: 'right' });

// 居中對齊
pdf.text(title, titleX, titleY, { align: 'center' });
```

### 座標轉換

```javascript
const pxToMm = (px) => (px / 595) * 210;
const pyToMm = (py) => (py / 842) * 297;
```

## ✅ 驗證清單

修復後，生成 PDF 應該符合：
- [ ] Logo 在左上角 (14.12mm, 14.12mm)
- [ ] 公司資訊在右上角，右對齊到 195.88mm
- [ ] 標題居中在頁面中央
- [ ] 發票資訊在左側 (14.12mm)
- [ ] 客戶資訊在右側，右對齊到 195.88mm
- [ ] 總計金額在右下方，右對齊到 195.88mm
- [ ] 所有元件不超出頁面邊界

---

**最後更新**: 2025-01-XX
**版本**: 1.0
